# Legacy community package — publish only on community-v* tags.
# Not part of AgentSociety2 active CI/security scope (see .github/agentsociety2-scope.yml).

name: Upload Python Package - Agentsociety Community

on:
  push:
    tags:
      # Publish on any tag starting with a `v`, e.g. v1.2.3
      - community-v*.*.*

permissions:
  contents: read

jobs:
  pypi:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    # Environment and permissions trusted publishing.
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v9.0.0
      - run: cd packages/agentsociety-community && uv build
      - run: uv publish --trusted-publishing always
